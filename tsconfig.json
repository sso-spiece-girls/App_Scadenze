{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  // The supabase/ folder contains Deno edge functions, not Vite code:
  // keep them out of every TS project here (they typecheck in the Deno
  // runtime via `supabase functions`).
  "exclude": ["node_modules", "dist", "supabase"]
}